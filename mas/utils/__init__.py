"""RM Copilot — extracted stable modules.

The orchestration graph and Gradio UI still live in ``rm_copilot.ipynb``;
only the stable, pure / infrastructure layers are packaged here so they can
be unit-tested and imported without running the notebook end-to-end.
"""
