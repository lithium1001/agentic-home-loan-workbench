# Agentic Home Loan Workbench — dependencies for the FastAPI web app.
#
# Install with:  py -m pip install -r mas/requirements.txt
#
# Versions are pinned (==) to the set this app was verified against, so a fresh
# clone installs a combination known to work. No database — nothing else needed.

# ── web server ──────────────────────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.48.0
starlette==1.2.1
pydantic==2.12.5
pydantic-core==2.41.5
typing-extensions==4.15.0

# ── agent / LLM stack ───────────────────────────────────────────────────────
langchain-core==1.4.0
langchain-openai==1.2.2
langgraph==1.2.4
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langsmith==0.8.9
openai==2.26.0
tiktoken==0.13.0
tenacity==8.2.3
httpx==0.28.1

# ── data ────────────────────────────────────────────────────────────────────
# numpy MUST stay <2: pandas 2.1.1 is compiled against the numpy 1.x ABI, and on
# a host with numpy 2.x preinstalled (e.g. Colab) importing pandas fails with
# "numpy.dtype size changed ... binary incompatibility".
numpy==1.26.4
pandas==2.1.1
python-dotenv==1.2.2
urllib3==1.26.9

# ── policy RAG — the search_policy tool over the t&c/ PDFs ──────────────────
# BM25 sparse retrieval, so no embeddings or extra API key. pypdf reads the PDFs.
rank_bm25==0.2.2
pypdf==6.13.2

# ── letter PDF — the draft_letter tool ──────────────────────────────────────
# Imported at startup, so the app will not launch without it.
reportlab==4.5.1
